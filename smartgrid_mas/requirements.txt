# Deprecated — the canonical requirements file is at the repo root.
# Kept as a pointer so existing tooling that runs
#   pip install -r smartgrid_mas/requirements.txt
# still works.
-r ../requirements.txt
